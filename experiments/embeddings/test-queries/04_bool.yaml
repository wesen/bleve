query:
  bool:
    must:
      - match:
          field: content
          value: fox
          boost: 1.0
    should:
      - vector:
          field: vector
          text: "animal jumping"
          model: all-minilm
          k: 10
          boost: 2.0
    minimum_should_match: 0
options:
  size: 10
  highlight:
    fields: [content]
  sort:
    - field: _score
      desc: true 